import 'package:flutter/material.dart';

class IntroduceText extends StatelessWidget {
  const IntroduceText({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(
        'data',
        style: TextStyle(fontSize: 27, color: Colors.red),
      ),
    );
  }
}
